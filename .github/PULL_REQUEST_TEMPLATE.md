## What does this change?

<!-- A short description, and the issue it closes if there is one. -->

Closes #

## Why?

<!-- The problem this solves. Skip if it is obvious from the description. -->

## Checklist

- [ ] `make check` passes (`ruff check`, `ruff format --check`, `pytest`)
- [ ] New behaviour in `mover.py`, `config.py`, or `macos.py` has a test
- [ ] `CHANGELOG.md` has an entry under `## [Unreleased]`
- [ ] Only `app.py` imports `rumps` (see CONTRIBUTING.md)
- [ ] No new background threads that touch menu items or show dialogs

## How did you test it?

<!-- Menu bar apps are hard to test automatically. Say what you clicked. -->
